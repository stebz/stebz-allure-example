# Example of Stebz integration with Allure

Content:

* API tests using RestAssured located in `org.stebz.example.allure.restassured`
* Web tests using Selenide located in `org.stebz.example.allure.selenide`
